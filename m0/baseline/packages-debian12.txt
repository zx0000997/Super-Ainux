# Required / recommended packages for M0 baseline (Debian 12 bookworm).
# Install: sudo xargs -a packages-debian12.txt apt-get install -y
# Lines starting with # are ignored by xargs if you strip them first — use:
#   grep -v '^#' packages-debian12.txt | grep -v '^$' | sudo xargs apt-get install -y

ca-certificates
curl
jq
nftables
openssh-server
podman
python3
python3-jsonschema
rsync
sudo
wget

# Podman 4.x (Debian 12+) includes: podman compose
# If your distro lacks it, install podman-compose from pip (see docs/M0_DEPLOYMENT.zh.md).

# Optional: SOPS for encrypted secrets in Git
# age
# sops
